//import files:

//add file exports to this exports object:
module.exports = {

}


/*
const { ApolloError } = require("apollo-server");

module.exports = async (_, { *input variables* }, { models }) => {
  try {

  } catch (error) {
    throw new ApolloError(error);
  }
};

*/